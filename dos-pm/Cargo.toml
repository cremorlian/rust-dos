[package]
name = "dos-pm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "DOS protected-mode (DPMI) runtime: embedded-io console, exit, heap"

[dependencies]
embedded-io = "=0.7.1"

[lints]
workspace = true